# Icon Rules

All SVG icons must be imported from src/components/ui/icons.tsx.
Do not inline SVG paths in component files.
Do not install new icon libraries without checking icons.tsx first.

Every icon must have aria-hidden="true" when it is decorative.
Every icon-only interactive element must have an aria-label on the parent button.
